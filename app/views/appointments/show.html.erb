<h4><%= @appointment.appointment_datetime.strftime("%B %-d, %Y at %k:%M") %></h4><br>

<%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor) %><br>
<%= link_to @appointment.patient.name, patient_path(@appointment.patient) %>